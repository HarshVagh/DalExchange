import React from 'react';
import Layout from './Layout';

const LandingPage = () => {
    return (
        <Layout>
            <h2>Welcome to Dal Exchange</h2>
            <p>This is the landing page.</p>
        </Layout>
    );
};

export default LandingPage;
